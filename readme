npm install expo-cli --global       //on new machine

expo init tech_stach
cd tech_stach
expo start

//scan QR on chrome path change App.js from main dir to ./src by changing nodemodule of expo in AppEntry.js
// decided to keep app.js out


1. Add one or more files to staging (index):	
git add *

2. Commit changes to head (but not yet to the remote repository):	
git commit -m "Commit message"

3. Create a new branch and switch to it:	
git checkout -b <branchname>

4. Push the branch to your remote repository, so others can use it:	
git push origin <branchname>
 Push all branches to your remote repository:	
git push --all origin

5. Switch from one branch to another:	
git checkout <branchname>

6. List all the branches in your repo, and also tell you what branch you're currently in:	
git branch